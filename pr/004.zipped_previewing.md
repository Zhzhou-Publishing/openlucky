# 压缩预览功能

## 压缩文件功能

在 PhotoDirectory 中复制文件到 workingDirectory 的时候：

1. 将所有图片压缩到 800 长边，高度按比例缩放，然后再放到 workingDirectory 中，如果长边小于 800，则不缩放直接复制到 workingDirectory 中。
2. 缩放使用命令：openlucky tool resize -i <input_file_path> -o <output_file_path> -v 800
3. outputDirectory 设置为 workingDirectory 的子目录 output。
