# 标题更新状态

## PhotoDirectory.vue 页面标题更新状态

1. 当选择了目录、正在执行从 originalDirectory 复制到 workingDirectory 的过程中，即 `prepare-working-directory-from-selected` 这个事件里面，resizeImage 的每个 inputPath 都更改窗口标题，窗口标题增加这个 inputPath 做后缀，如【OpenLucky Desktop App - D:\test-path\test-001.tif】。

2. 执行到 `working-directory-from-selected-prepared` 说明 workingDirectory 已经完成了过后又恢复原来的窗口标题【OpenLucky Desktop App】。

## PhotoGallery.vue 页面标题更新状态

1. 当点击了 Apply 按钮过后，在执行完成之前，标题里都加一个固定后缀，如【OpenLucky Desktop App - Applying】。

2. 点击了 Save All 过后取消显示为【OpenLucky Desktop App - Saving】，改为显示成【OpenLucky Desktop App - Saving D:\test-path\test-001.tif】，利用 `preset-to-batch-progress` 提供的 progress 和 data 信息来改标题。

3. 这两个后缀文案尽量使用 i18n。请使用现有的 vue-i18n 的方式实现，实现不了就放弃，禁止引入新的 i18n 方案。

4. Apply 和 Save All 的点击主函数执行完毕过后，恢复原来的窗口标题【OpenLucky Desktop App】。

## PhotoEdit.vue 页面标题更新状态

1. 当